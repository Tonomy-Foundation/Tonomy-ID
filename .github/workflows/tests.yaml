name: Test - Pull Requests (not to master)

on:
  pull_request:
    branches-ignore:
      - master

jobs:
  build:
    runs-on: ubuntu-20.04

    services:
      ganache:
        image: trufflesuite/ganache-cli:latest
        ports:
          - 7545:8545

    steps:
      - name: 🏗 Setup repo
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: yarn --immutable

      - name: Wait for Ganache to be ready
        run: |
          echo "Waiting for Ganache to be ready..."
          for i in `seq 1 30`; do
            if curl -s -X POST --data '{"jsonrpc":"2.0","method":"web3_clientVersion","params":[],"id":1}' http://127.0.0.1:7545 > /dev/null; then
              echo "Ganache is running"
              break
            fi
            echo "Waiting for Ganache to be ready... attempt $i"
            sleep 1
          done
          if ! curl -s -X POST --data '{"jsonrpc":"2.0","method":"web3_clientVersion","params":[],"id":1}' http://127.0.0.1:7545 > /dev/null; then
            echo "Ganache did not start in time"
            exit 1
          fi

      - name: 📦 Build, test and lint
        uses: actions/setup-node@v4
        with:
          node-version: 18.12.1

      - run: corepack enable

      - name: Run tests
        run: yarn run test

      - name: Run lint
        run: yarn run lint

      - name: Run type check
        run: yarn run typeCheck
