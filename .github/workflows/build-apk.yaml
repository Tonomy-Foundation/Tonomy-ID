name: Build App locally and run security analysis

on:
  pull_request:
    branches-ignore:
      - master

jobs:
  build-android:
    runs-on: ubuntu-latest
    env:
      EXPO_NODE_ENV: demo
      EXPO_PLATFORM: android
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
    container: cimg/android:2022.12.1-ndk

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 🚀 Build and publish tonomy app
        uses: actions/setup-node@v3
        with:
          node-version: 18.12.1
      - run: corepack enable
      - run: yarn --immutable
      - run: yarn run build:prepare
      - run: npx eas build --clear-cache --profile demo-apk --platform android --local

      - name: Find APK file and rename it
        run: find . -name "*.apk" -exec mv {} TonomyID.apk \;

      - name: Archive Android APK
        uses: actions/upload-artifact@v3
        with:
          name: TonomyID-Android
          path: ./TonomyID.apk

  security-test4:
    runs-on: ubuntu-latest
    needs: build-android

    steps:
      - uses: actions/checkout@v3

      - name: Download APK file
        uses: actions/download-artifact@v2
        with:
          name: TonomyID-Android
          path: ./mobsf/TonomyID.apk

      - name: Install MobSF
        run: docker build ./mobsf -t mobsf
      
      - name: Run MobSF
        run: docker run -u root -v $PWD/mobsf:/github/workspace/:rw -e GITHUB_WORKSPACE=/github/workspace -e INPUT_FILE_NAME=TonomyID.apk -e OUTPUT_FILE_NAME=mobsf-report -e SCAN_TYPE=apk mobsf
  
      - name: Check for file
        run: ls -la ./mobsf

      - name: Upload MobSF Analysis PDF Result
        uses: actions/upload-artifact@v3
        with:
          name: mobsf-report
          path: ./mobsf/mobsf-report.pdf

      - name: Upload MobSF Analysis JSON Result
        uses: actions/upload-artifact@v3
        with:
          name: mobsf-report
          path: ./mobsf/mobsf-report.json
  